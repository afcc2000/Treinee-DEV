<?php
/**
 * Exercício 13 — Cálculo de troco
 * Microcurso: MC2 — Lógica de programação I (variáveis, tipos, operadores)
 *
 * Conceitos praticados: subtração aplicada a um problema real
 *
 * Enunciado:
 * Leia o valor de uma compra e o valor pago pelo cliente, e calcule o troco.
 * Por enquanto não precisa se preocupar com o caso de pagamento
 * insuficiente.
 *
 * Entrada:
 * Duas linhas: valor da compra, valor pago. Exemplo:
 * 47.50
 * 50
 *
 * Saída esperada:
 * Troco: 2.5
 */
$valorCompra = (float) trim(fgets(STDIN)); // valor da compra
$valorPago = (float) trim(fgets(STDIN)); // valor pago pelo cliente

// TODO: escreva sua solução abaixo.
