<?php
/**
 * Exercício 02 — Soma de dois números
 * Microcurso: MC2 — Lógica de programação I (variáveis, tipos, operadores)
 *
 * Conceitos praticados: variáveis numéricas, operador de soma, conversão de tipo (cast)
 *
 * Enunciado:
 * Leia dois números inteiros e exiba a soma dos dois.
 *
 * Entrada:
 * Duas linhas, uma para cada número. Exemplo:
 * 4
 * 7
 *
 * Saída esperada:
 * A soma é 11
 */
$a = (int) trim(fgets(STDIN)); // primeiro número
$b = (int) trim(fgets(STDIN)); // segundo número

// TODO: escreva sua solução abaixo.
