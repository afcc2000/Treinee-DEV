<?php
/**
 * Exercício 16 — Troca de valores (com variável auxiliar)
 * Microcurso: MC2 — Lógica de programação I (variáveis, tipos, operadores)
 *
 * Conceitos praticados: atribuição, variável temporária
 *
 * Enunciado:
 * Leia dois valores e troque o conteúdo das duas variáveis entre si, usando
 * uma terceira variável auxiliar. Exiba os dois valores antes e depois da
 * troca.
 *
 * Entrada:
 * Duas linhas. Exemplo:
 * 3
 * 8
 *
 * Saída esperada:
 * Antes: a=3, b=8
 * Depois: a=8, b=3
 */
$a = (int) trim(fgets(STDIN)); // primeiro valor
$b = (int) trim(fgets(STDIN)); // segundo valor

// TODO: escreva sua solução abaixo.
