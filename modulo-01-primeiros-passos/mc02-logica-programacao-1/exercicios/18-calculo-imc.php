<?php
/**
 * Exercício 18 — Cálculo do IMC
 * Microcurso: MC2 — Lógica de programação I (variáveis, tipos, operadores)
 *
 * Conceitos praticados: fórmula real usada no dia a dia, potenciação
 *
 * Enunciado:
 * Leia o peso (em kg) e a altura (em metros) de uma pessoa e calcule o IMC
 * (peso dividido pela altura ao quadrado). Só o cálculo por enquanto —
 * classificar o resultado é o próximo microcurso.
 *
 * Entrada:
 * Duas linhas: peso, altura. Exemplo:
 * 70
 * 1.75
 *
 * Saída esperada:
 * IMC: aproximadamente 22.86
 */
$peso = (float) trim(fgets(STDIN)); // peso em kg
$altura = (float) trim(fgets(STDIN)); // altura em metros

// TODO: escreva sua solução abaixo.
